function [delW1, delW2, delW3, del_bias1, del_bias2, del_bias3] = Backpropagation(OH, AH, OY, AY, ax, t, net)

 %delW
 delW3 = (t' - AY) * AH';
 del_bias3 = sum(t' - AY);
 
 for  i = 1:1:size(ax,1)
     %---Compute change for W1
     delW3 = delW3;
     
 end




end

